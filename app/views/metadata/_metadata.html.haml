.mb-4.border.rounded.overflow-hidden.bg-light-subtle
  .p-3
    %h5.mb-2.d-flex.align-items-center.flex-wrap
      %i.bi.bi-database-fill.me-2
      = metadatum.title

      - if metadatum.topics.present?
        - topics_text = metadatum.topics.map { |s| s.to_s.titleize }.join(', ')
        %span.ms-2.text-secondary.fw-semibold.small= "• #{topics_text}"

    %p.text-muted= metadatum.description

    - metadatum.keywords.each do |tag|
      %span.badge.bg-secondary.mx-1.text-capitalize= tag

  .border-top.px-3.py-2.d-flex.justify-content-between.align-items-center
    .text-muted.small Last Updated: #{metadatum.updated_at}
    = link_to metadatum, class: "btn btn-aedg-primary d-flex align-items-center" do 
      View
      %i.bi.bi-arrow-right.ms-1