%div{ data: { controller: "chart" } }
  %div.charts-row
    %div.chart-container
      %canvas{ data: { chart_target: "barChart" }, id: "barChart" }
    %div.chart-container
      %canvas{ data: { chart_target: "pieChart" }, id: "pieChart" }

  %div.chart-container
    %canvas{ data: { chart_target: "bubbleChart" }, id: "bubbleChart" }

.text-center
  .card
    .card-body.p-5
      %h1.fw-bold Community Summaries
      %p.lead.w-75.mx-auto
        The Community Summary feature provides key insights into communities, covering geography, population and employment, fuel prices, electric rates, and more.
      %br
      = render partial: "dropdown", locals: { communities: @communities, current_community: nil }
