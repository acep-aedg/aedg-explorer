/ --- SEARCH BAR ---
%div.text-center
  = form_with url: grids_path, method: :get do |f|
    .input-group.bg-white.mx-auto.search-bar.ps-4
      = f.text_field :q, value: @query, class: "form-control border-0", placeholder: "Search for a grid..."
      - if @query.present?
        = link_to grids_path, class: 'btn btn-white border-0' do
          %i.bi.bi-x.text-secondary
      = f.button name: nil, class: 'btn btn-white border-0' do
        %i.bi.bi-search.text-muted
