= form_tag search_advanced_path, method: :get, id: 'filter-form', class: 'd-none', data: { turbo_action: "advance" } do
  = hidden_field_tag :q, params[:q]
  = hidden_field_tag :expanded, params[:expanded], data: { offcanvas_group_target: "tracker" }