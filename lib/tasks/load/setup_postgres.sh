#!/bin/bash

# Note: 
# Some of these operations require superuser priveleges (create user, create database)
# Suss out later

# Note:
# Password and credentials are stored in ~/.pgpass file


# Connect as superuser
# Drop database if exists `aedg`
# Drop user if exists`dev`
# Create user `dev`
# Create database `aedg`
# Install spatial plugin (PostGIS)

















